{
  "name": "calledit",
  "version": "0.1.0",
  "private": true,
  "description": "CalledIt Telegram prediction bot backend",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace backend",
    "backup": "node scripts/backup-sqlite.mjs",
    "restore": "node scripts/restore-sqlite.mjs",
    "txline:activate-devnet": "node scripts/txline-activate-devnet.mjs",
    "market:initialize-devnet": "npm run build --workspace backend && node backend/dist/market/initialize.js",
    "market:smoke-devnet": "npm run build --workspace backend && node backend/dist/market/devnetSmoke.js",
    "market:smoke-txline-cpi-devnet": "npm run build --workspace backend && node backend/dist/market/txlineCpiSmoke.js"
  },
  "workspaces": [
    "backend"
  ],
  "overrides": {
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.4",
    "tweetnacl": "^1.0.3"
  }
}
